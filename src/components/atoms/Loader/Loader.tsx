import React from 'react';
import { StyledContainer, StyledLoader } from './Loader.styles';

const Loader = () => (
  <StyledContainer>
    <StyledLoader />
  </StyledContainer>
);
export default Loader;
